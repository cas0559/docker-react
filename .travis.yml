language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t cas0559/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true cas0559/docker-react npm run test
